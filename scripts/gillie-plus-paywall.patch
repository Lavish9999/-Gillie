diff --git a/index.html b/index.html
index 8c6fd3f..9f39d12 100644
--- a/index.html
+++ b/index.html
@@ -5,7 +5,7 @@
 <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, user-scalable=no">
 <meta name="apple-mobile-web-app-capable" content="yes">
 <meta name="apple-mobile-web-app-status-bar-style" content="default">
-<meta name="theme-color" content="#075E5A">
+<meta name="theme-color" content="#E8F2EF">
 <meta name="description" content="Gillie is a mobile-first quit-vaping companion with a supportive aquatic buddy.">
 <meta name="apple-mobile-web-app-title" content="Gillie">
 <title>Gillie — Quit Vaping</title>
@@ -50,7 +50,7 @@
   --font-body:"Figtree", -apple-system, "Helvetica Neue", system-ui, sans-serif;
 }
 *{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
-html{height:100%}
+html{height:100%;background:var(--bg)}
 body{
   font-family:var(--font-body);
   background:var(--bg);
@@ -658,41 +658,74 @@ nav#tabs button.on{color:var(--ink)}
   background:rgba(226,83,111,.12);color:var(--coral-deep);font-size:24px;font-weight:800;
 }
 .field input.text-sheet-input{font-size:20px;font-weight:800}
-.plus-hero{
+#plus-overlay .sheet{
+  padding-left:16px;padding-right:16px;
+  background:linear-gradient(180deg,#DFF2EF 0%,#ECF6F2 42%,#F8FCFA 100%);
+}
+#plus-overlay .sheet>.sheet-close{display:none}
+.plus-tank-hero{
+  position:relative;min-height:360px;border-radius:26px;padding:18px 16px 16px;margin-bottom:12px;
+  overflow:hidden;color:#fff;box-shadow:0 22px 54px rgba(5,35,35,.26), inset 0 0 0 1px rgba(255,255,255,.30);
   background:
-    radial-gradient(circle at 84% 6%, rgba(130,244,224,.24), transparent 34%),
-    linear-gradient(145deg,#123F39,#072824);color:#fff;border-radius:22px;
-  padding:20px;margin-bottom:14px;box-shadow:0 16px 38px rgba(8,42,38,.22);position:relative;overflow:hidden;
+    radial-gradient(circle at 24% 20%, rgba(229,255,248,.34), transparent 24%),
+    radial-gradient(circle at 78% 18%, rgba(114,232,220,.26), transparent 30%),
+    linear-gradient(180deg,#0E5B58 0%,#0A3738 62%,#D9C48D 63%,#CBAF73 100%);
   touch-action:none;
 }
-.plus-hero::after{content:"";position:absolute;right:-38px;top:-38px;width:120px;height:120px;border-radius:50%;background:rgba(255,255,255,.10)}
-.plus-hero h2{color:#fff;margin-bottom:7px}
-.plus-hero .sub{color:rgba(255,255,255,.78);margin-bottom:0}
-.plus-proof{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:0 0 14px}
-.plus-proof div{background:rgba(255,255,255,.76);border:1px solid rgba(211,227,222,.8);border-radius:15px;padding:10px 9px}
-.plus-proof b{display:block;font-family:var(--font-display);font-size:17px;line-height:1;color:var(--ink)}
-.plus-proof span{display:block;font-size:10.5px;line-height:1.15;color:var(--ink-faint);font-weight:800;margin-top:4px}
-.plus-now{display:grid;gap:8px;margin:0 0 14px}
-.plus-now div{background:rgba(255,255,255,.80);border:1px solid rgba(211,227,222,.8);border-radius:15px;padding:11px 12px;font-size:13px;font-weight:750;color:var(--ink-soft);line-height:1.35}
+.plus-tank-hero::before{
+  content:"";position:absolute;inset:0;pointer-events:none;
+  background:
+    linear-gradient(110deg, rgba(255,255,255,.38) 0 10%, transparent 23% 76%, rgba(0,25,28,.22) 100%),
+    repeating-linear-gradient(96deg, transparent 0 78px, rgba(255,255,255,.10) 79px 132px, transparent 133px 210px);
+  mix-blend-mode:soft-light;
+}
+.plus-ray{position:absolute;top:-30px;width:86px;height:280px;background:linear-gradient(180deg,rgba(255,255,230,.30),transparent);filter:blur(6px);opacity:.72;transform-origin:50% 0;animation:plusRay 9s ease-in-out infinite}
+.plus-ray.r1{left:38px;transform:rotate(10deg)}
+.plus-ray.r2{right:64px;animation-delay:-3.5s;transform:rotate(-14deg)}
+@keyframes plusRay{0%,100%{opacity:.38;transform:rotate(8deg) translateX(0)}50%{opacity:.76;transform:rotate(-4deg) translateX(10px)}}
+.plus-bubbles{position:absolute;inset:0;pointer-events:none}
+.plus-bubbles i{position:absolute;bottom:22%;border:1.5px solid rgba(255,255,255,.62);border-radius:50%;opacity:.72;animation:plusBubble 5.8s linear infinite}
+.plus-bubbles i:nth-child(1){left:14%;width:10px;height:10px;animation-delay:-1.1s}
+.plus-bubbles i:nth-child(2){left:27%;width:6px;height:6px;animation-delay:-3s}
+.plus-bubbles i:nth-child(3){right:22%;width:12px;height:12px;animation-delay:-2.2s}
+.plus-bubbles i:nth-child(4){right:12%;width:7px;height:7px;animation-delay:-4.6s}
+@keyframes plusBubble{to{transform:translateY(-260px) translateX(18px);opacity:0}}
+.plus-hero-top{position:relative;z-index:2;display:flex;justify-content:space-between;gap:12px;align-items:flex-start}
+.plus-kicker{display:inline-flex;align-items:center;gap:7px;padding:7px 10px;border-radius:999px;background:rgba(255,255,255,.15);font-size:11px;font-weight:900;letter-spacing:.11em;text-transform:uppercase}
+.plus-hero-top h2{font-size:34px;line-height:.95;color:#fff;margin:12px 0 8px;max-width:220px}
+.plus-hero-top p{max-width:250px;color:rgba(255,255,255,.80);font-weight:700;font-size:14.5px;line-height:1.35}
+.plus-close-pearl{width:42px;height:42px;border-radius:50%;display:grid;place-items:center;background:rgba(255,255,255,.18);box-shadow:inset 0 0 0 1px rgba(255,255,255,.28);font-weight:900;color:#fff}
+.plus-mascot-wrap{position:absolute;z-index:2;left:50%;top:136px;width:190px;transform:translateX(-50%);filter:drop-shadow(0 18px 22px rgba(0,22,24,.20));animation:plusFloat 4.6s ease-in-out infinite}
+.plus-mascot-wrap svg{width:100%;display:block}
+@keyframes plusFloat{0%,100%{transform:translateX(-50%) translateY(0) rotate(-1deg)}50%{transform:translateX(-50%) translateY(-10px) rotate(1deg)}}
+.plus-stat-chips{position:absolute;z-index:3;left:13px;right:13px;bottom:14px;display:grid;grid-template-columns:repeat(3,1fr);gap:8px}
+.plus-stat-chip{padding:10px 8px;border-radius:16px;background:rgba(255,255,255,.78);backdrop-filter:blur(12px);color:#143B37;box-shadow:0 10px 22px rgba(0,32,34,.12);border:1px solid rgba(255,255,255,.46)}
+.plus-stat-chip b{display:block;font-family:var(--font-display);font-size:17px;line-height:1}
+.plus-stat-chip span{display:block;margin-top:4px;font-size:10.5px;line-height:1.08;font-weight:900;color:var(--ink-faint);text-transform:uppercase;letter-spacing:.05em}
+.plus-proof{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:0 0 12px}
+.plus-proof div{background:rgba(255,255,255,.82);border:1px solid rgba(211,227,222,.86);border-radius:17px;padding:11px 9px;box-shadow:var(--shadow-soft)}
+.plus-proof b{display:block;font-family:var(--font-display);font-size:16px;line-height:1;color:var(--ink)}
+.plus-proof span{display:block;font-size:11.5px;line-height:1.15;color:var(--ink-faint);font-weight:800;margin-top:5px}
+.plus-now{display:grid;gap:8px;margin:0 0 12px}
+.plus-now div{background:rgba(255,255,255,.82);border:1px solid rgba(211,227,222,.82);border-radius:17px;padding:12px 13px;font-size:13.5px;font-weight:750;color:var(--ink-soft);line-height:1.36;box-shadow:var(--shadow-soft)}
 .plus-now strong{color:var(--ink)}
-.plus-compare{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:0 0 14px}
-.plus-compare div{background:rgba(255,255,255,.80);border:1px solid rgba(211,227,222,.85);border-radius:15px;padding:11px 10px;font-size:12.5px;font-weight:750;color:var(--ink-soft);line-height:1.35}
-.plus-compare b{display:block;font-family:var(--font-display);font-size:16px;color:var(--ink);margin-bottom:4px}
-.plus-compare .plus-side{border-color:rgba(223,174,63,.55);box-shadow:0 8px 18px rgba(223,174,63,.10)}
-.plus-perks{
-  background:rgba(255,255,255,.88);border-radius:var(--radius-sm);padding:8px 16px;box-shadow:var(--shadow-soft);
-  margin-bottom:16px;font-size:14.5px;font-weight:700;line-height:1.48;border:1px solid rgba(255,255,255,.72);
-}
-.plus-perks div{display:flex;gap:10px;padding:10px 0;border-bottom:1px solid var(--bg-deep)}
-.plus-perks div span:first-child{color:var(--good);font-weight:900}
-.plus-perks div:last-child{border-bottom:none}
-.price-card{
-  border-radius:18px;background:var(--card);box-shadow:var(--shadow-soft);padding:12px;margin-bottom:10px;
-  border:2px solid transparent;
-}
-.price-card.best{border-color:var(--gold)}
-.price-card .label{font-size:11px;font-weight:800;color:var(--gold);letter-spacing:.08em;text-transform:uppercase;margin-bottom:8px}
-.price-card .btn{margin:0}
+.plus-plans{display:grid;gap:9px;margin:0 0 10px}
+.plus-plan{
+  display:grid;grid-template-columns:1fr auto;align-items:center;gap:10px;text-align:left;
+  border-radius:19px;background:rgba(255,255,255,.88);border:1.5px solid rgba(211,227,222,.95);
+  padding:13px 14px;box-shadow:var(--shadow-soft);transition:transform .14s ease,border-color .18s ease,box-shadow .18s ease;
+}
+.plus-plan:active{transform:scale(.985)}
+.plus-plan.on{border-color:var(--gold);box-shadow:0 12px 26px rgba(223,174,63,.18), inset 0 0 0 1px rgba(223,174,63,.32)}
+.plus-plan .name{font-weight:900;color:var(--ink)}
+.plus-plan .note{font-size:12px;font-weight:800;color:var(--ink-faint);margin-top:3px}
+.plus-plan .price{font-family:var(--font-display);font-size:21px;font-weight:900;color:var(--ink);white-space:nowrap}
+.plus-plan .price small{font-family:var(--font-body);font-size:11px;font-weight:900;color:var(--ink-faint);margin-left:2px}
+.plus-plan .badge{display:inline-flex;margin-left:7px;padding:3px 7px;border-radius:999px;background:rgba(223,174,63,.18);color:#896515;font-size:10px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;vertical-align:middle}
+.plus-cta{margin-top:8px;background:linear-gradient(135deg,#123F39,#071F1D);box-shadow:0 13px 28px rgba(7,31,29,.20)}
+.plus-restore-row{display:flex;justify-content:center;gap:14px;align-items:center;margin-top:12px;font-size:12px;font-weight:800;color:var(--ink-faint)}
+.plus-restore-row button{text-decoration:underline;text-underline-offset:3px;font-weight:900;color:var(--ink-soft)}
+.plus-legal{font-size:11.5px;color:var(--ink-faint);text-align:center;margin:10px 10px 0;line-height:1.35}
 .field textarea{
   width:100%;min-height:86px;resize:vertical;padding:14px 16px;border-radius:var(--radius-sm);
   border:1.5px solid var(--line);background:var(--card);color:var(--ink);
@@ -767,12 +800,13 @@ nav#tabs button.on{color:var(--ink)}
 
 /* milestone toast */
 #toast{
-  position:fixed;top:calc(14px + env(safe-area-inset-top));left:50%;transform:translateX(-50%) translateY(-140%);
+  position:fixed;top:calc(52px + env(safe-area-inset-top));left:50%;transform:translateX(-50%) translateY(-160%);
   z-index:80;background:var(--ink);color:#fff;border-radius:18px;padding:14px 20px;
   font-weight:700;font-size:14.5px;box-shadow:var(--shadow);width:calc(100% - 40px);max-width:400px;
-  transition:transform .45s cubic-bezier(.2,.9,.3,1);display:flex;gap:12px;align-items:center;
+  opacity:0;pointer-events:none;
+  transition:transform .35s cubic-bezier(.2,.9,.3,1), opacity .25s ease;display:flex;gap:12px;align-items:center;
 }
-#toast.show{transform:translateX(-50%) translateY(0)}
+#toast.show{transform:translateX(-50%) translateY(0);opacity:1}
 #toast .em{font-size:22px}
 
 @media (prefers-reduced-motion: reduce){
@@ -1256,30 +1290,32 @@ nav#tabs button.on{color:var(--ink)}
 <div class="overlay" id="plus-overlay" hidden>
   <div class="sheet">
     <div class="grab"></div>
-    <div class="plus-hero">
-      <h2>Gillie Plus</h2>
-      <p class="sub">A calmer quit plan that learns your patterns. Core tracking stays free.</p>
-    </div>
-    <div class="plus-proof">
-      <div><b>Plan</b><span>daily moves</span></div>
-      <div><b>Predict</b><span>danger hours</span></div>
-      <div><b>Recover</b><span>after slips</span></div>
-    </div>
-    <div class="plus-compare">
-      <div><b>Free</b>Track your streak, check in, use SOS, and protect the next clean minute.</div>
-      <div class="plus-side"><b>Plus</b>Get a plan for danger hours, triggers, slips, savings, and Coach missions.</div>
+    <div class="plus-tank-hero">
+      <div class="plus-ray r1"></div>
+      <div class="plus-ray r2"></div>
+      <div class="plus-bubbles"><i></i><i></i><i></i><i></i></div>
+      <div class="plus-hero-top">
+        <div>
+          <div class="plus-kicker" id="plus-kicker">The tank gets smarter</div>
+          <h2 id="plus-title">Gillie Plus</h2>
+          <p id="plus-subtitle">Unlock the quit plan that watches your patterns before the urge gets loud.</p>
+        </div>
+        <button class="plus-close-pearl" id="plus-soft-close" type="button" aria-label="Close Gillie Plus">×</button>
+      </div>
+      <div class="plus-mascot-wrap">
+        <svg viewBox="0 0 200 160" id="plus-mascot-svg" aria-hidden="true"></svg>
+      </div>
+      <div class="plus-stat-chips" id="plus-stat-chips"></div>
     </div>
+    <div class="plus-proof" id="plus-proof"></div>
     <div class="plus-now" id="plus-now"></div>
-    <div class="plus-perks" id="plus-perks"></div>
-    <div class="price-card best">
-      <div class="label">Best value</div>
-      <button class="btn" id="plus-yearly">—</button>
-    </div>
-    <div class="price-card">
-      <button class="btn ghost" id="plus-monthly">—</button>
+    <div class="plus-plans" id="plus-plans"></div>
+    <button class="btn plus-cta" id="plus-purchase">Unlock Gillie Plus</button>
+    <div class="plus-restore-row">
+      <span>Already subscribed?</span>
+      <button id="plus-restore" type="button">Restore purchases</button>
     </div>
-    <button class="btn ghost" id="plus-restore" style="box-shadow:none;margin-top:2px">Restore purchases</button>
-    <p style="font-size:11.5px;color:var(--ink-faint);text-align:center;margin-top:12px">Purchase flow connects in the App Store build.</p>
+    <p class="plus-legal" id="plus-legal">Purchase flow connects in the App Store build. Core quit tracking stays free.</p>
   </div>
 </div>
 
@@ -1392,17 +1428,24 @@ const CONFIG = {
   rewards: { checkin: 10, cravingBeaten: 5, cravingLoggedAfterSlip: 5, milestone: 25 },
   sos: { rewardDailyCap: 3, rewardCooldownMins: 45, minBreathSecs: 16, followupDelayMins: 10 },
   plus: {
-    monthlyLabel: "$4.99 / month",
-    yearlyLabel: "$39.99 / year — best value",
+    kicker: "Paywall is the tank",
+    title: "Gillie Plus",
+    subtitle: "Your tank learns the danger hours, triggers, slips, and wins that shape your quit.",
+    cta: "Unlock Gillie Plus",
+    legal: "Core tracking, SOS, check-ins, streaks, and Gillie's tank stay free. Purchases run through the App Store build.",
+    products: {
+      yearly: { id: "gillie.plus.yearly", name: "Yearly", price: "$29.99", cadence: "/ year", note: "Best value for staying locked in.", badge: "Save 37%" },
+      monthly: { id: "gillie.plus.monthly", name: "Monthly", price: "$3.99", cadence: "/ month", note: "Full Plus access. Cancel anytime." },
+    },
     pearlMultiplier: 2,
-    perks: [
-      "A daily quit plan built from your check-ins, cravings, slips, and danger windows",
-      "Trigger playbooks for stress, boredom, scrolling, drinking, food, and social pressure",
-      "Danger-hour forecasts so you can prepare before the urge gets loud",
-      "Slip recovery that turns setbacks into a next-time plan instead of shame",
-      "Deeper insights: mood trend, win rate, best streak, trigger patterns, and lessons",
-      "Savings goals that turn old habit spending into something real",
-      "Premium tank themes, rare Gillie colors, tank mates, and 2x pearls",
+    pillars: [
+      { title: "Plan", copy: "Daily moves based on your check-ins" },
+      { title: "Predict", copy: "Danger-hour warnings before cravings spike" },
+      { title: "Recover", copy: "Slip reviews that turn setbacks into next steps" },
+    ],
+    valueCards: [
+      "Coach missions for your exact trigger patterns",
+      "Savings goals, deeper insights, rare tank themes, tank mates, and 2x pearls",
     ],
     // In the native build, set state.premium only after a verified App Store entitlement.
   },
@@ -1631,10 +1674,8 @@ const growthFor = (ms) => { let g = GROWTH_STAGES[0]; for (const st of GROWTH_ST
 const skinOf = (id) => SKINS.find((s) => s.id === id) || SKINS[0];
 const themeOf = (id) => THEMES.find((t) => t.id === id) || THEMES[0];
 
-const PLUS_PRODUCTS = {
-  monthly: "gillie.plus.monthly",
-  yearly: "gillie.plus.yearly",
-};
+const PLUS_PRODUCTS = Object.fromEntries(Object.entries(CONFIG.plus.products).map(([key, plan]) => [key, plan.id]));
+let selectedPlusPlan = "yearly";
 
 const purchaseBridge = () => window.Capacitor?.Plugins?.GilliePurchases || null;
 
@@ -1867,7 +1908,6 @@ $("#ob-hatch").onclick = () => {
   state.originalQuitAt = state.quitAt;
   save();
   enterMain();
-  toast("🐣", `${state.petName} hatched. Your quit is officially live.`);
 };
 
 /* ============================================================
@@ -3161,22 +3201,51 @@ function openPlus() {
   const danger = dangerWindow();
   const trigger = topTrigger();
   const profile = activeSubstance();
+  const streakDays = Math.max(0, Math.floor(currentStreakMs() / dayMs));
+  const money = lifetimeCleanMs() * costPerMs();
+  const saved = money >= 100 ? "$" + Math.round(money) : "$" + money.toFixed(2);
   const planLine = danger
     ? `Tonight's plan can protect ${danger.label}, your hottest logged window.`
     : trigger
       ? `Your plan can start with a counter-move for ${trigger.toLowerCase()}.`
       : `Your first plan starts with ${profile.name} cues, craving level, and one replacement routine.`;
+  $("#plus-kicker").textContent = CONFIG.plus.kicker;
+  $("#plus-title").textContent = CONFIG.plus.title;
+  $("#plus-subtitle").textContent = CONFIG.plus.subtitle;
+  $("#plus-mascot-svg").innerHTML = axoSVG(state.skin, state.hat, "happy", "plus");
+  $("#plus-stat-chips").innerHTML = [
+    { value: streakDays ? `${streakDays}d` : "Day 0", label: "Current streak" },
+    { value: saved, label: "Saved so far" },
+    { value: danger ? danger.label : "Tonight", label: "Watch window" },
+  ].map((chip) => `<div class="plus-stat-chip"><b>${escapeHTML(chip.value)}</b><span>${escapeHTML(chip.label)}</span></div>`).join("");
+  $("#plus-proof").innerHTML = CONFIG.plus.pillars
+    .map((pillar) => `<div><b>${escapeHTML(pillar.title)}</b><span>${escapeHTML(pillar.copy)}</span></div>`)
+    .join("");
   $("#plus-now").innerHTML = [
     `<div><strong>Today:</strong> ${escapeHTML(planLine)}</div>`,
-    `<div><strong>When it gets hard:</strong> Coach gives you a specific move instead of another generic tip.</div>`,
-    `<div><strong>Over time:</strong> Gillie turns check-ins, slips, cravings, and savings into a plan that feels personal.</div>`,
+    ...CONFIG.plus.valueCards.map((card) => `<div><strong>Plus:</strong> ${escapeHTML(card)}</div>`),
   ].join("");
-  $("#plus-perks").innerHTML = CONFIG.plus.perks.map((perk) => `<div><span>✓</span><span>${escapeHTML(perk)}</span></div>`).join("");
-  $("#plus-yearly").textContent = CONFIG.plus.yearlyLabel;
-  $("#plus-monthly").textContent = CONFIG.plus.monthlyLabel;
+  renderPlusPlans();
+  $("#plus-purchase").textContent = CONFIG.plus.cta;
+  $("#plus-legal").textContent = CONFIG.plus.legal;
   $("#plus-overlay").hidden = false;
 }
 $("#plus-open").onclick = openPlus;
+function renderPlusPlans() {
+  $("#plus-plans").innerHTML = Object.entries(CONFIG.plus.products).map(([key, plan]) => `
+    <button class="plus-plan ${selectedPlusPlan === key ? "on" : ""}" data-plus-plan="${key}" type="button">
+      <span>
+        <span class="name">${escapeHTML(plan.name)}${plan.badge ? `<span class="badge">${escapeHTML(plan.badge)}</span>` : ""}</span>
+        <span class="note">${escapeHTML(plan.note)}</span>
+      </span>
+      <span class="price">${escapeHTML(plan.price)}<small>${escapeHTML(plan.cadence)}</small></span>
+    </button>
+  `).join("");
+  $$("[data-plus-plan]").forEach((btn) => btn.onclick = () => {
+    selectedPlusPlan = btn.dataset.plusPlan;
+    renderPlusPlans();
+  });
+}
 async function requestPlusPurchase(productId) {
   const bridge = purchaseBridge();
   if (!bridge?.purchase) {
@@ -3195,6 +3264,10 @@ async function requestPlusPurchase(productId) {
     toast("👑", "Purchase was not completed.");
   }
 }
+function requestSelectedPlusPurchase() {
+  const plan = CONFIG.plus.products[selectedPlusPlan] || CONFIG.plus.products.yearly;
+  requestPlusPurchase(plan.id);
+}
 async function restorePlusPurchase() {
   const bridge = purchaseBridge();
   if (!bridge?.restorePurchases) {
@@ -3213,9 +3286,9 @@ async function restorePlusPurchase() {
     toast("👑", "Could not restore purchases right now.");
   }
 }
-$("#plus-yearly").onclick = () => requestPlusPurchase(PLUS_PRODUCTS.yearly);
-$("#plus-monthly").onclick = () => requestPlusPurchase(PLUS_PRODUCTS.monthly);
+$("#plus-purchase").onclick = requestSelectedPlusPurchase;
 $("#plus-restore").onclick = restorePlusPurchase;
+$("#plus-soft-close").onclick = () => closeSheetOverlay($("#plus-overlay"), true);
 
 /* ============================================================
    Settings
@@ -3404,6 +3477,7 @@ $$(".overlay").forEach((ov) => ov.addEventListener("click", (e) => {
 function syncOverlayLock() {
   const anyOpen = $$(".overlay").some((ov) => !ov.hidden);
   document.body.classList.toggle("sheet-open", anyOpen);
+  if (anyOpen) dismissToast();
 }
 const overlayLockObserver = new MutationObserver(syncOverlayLock);
 $$(".overlay").forEach((ov) => overlayLockObserver.observe(ov, { attributes: true, attributeFilter: ["hidden"] }));
@@ -3422,7 +3496,7 @@ function enableSheetDragDismiss() {
         return true;
       }
       const isPlus = ov?.id === "plus-overlay";
-      const onPlusTop = isPlus && !!target.closest(".plus-hero,.plus-proof");
+      const onPlusTop = isPlus && !!target.closest(".plus-tank-hero,.plus-proof");
       const nearTop = y - top < (isPlus ? 190 : 88);
       const interactive = target.closest("button,input,textarea,select,a,label");
       return (onPlusTop || (nearTop && !interactive)) && sheet.scrollTop <= 1;
@@ -3506,12 +3580,17 @@ enableSheetDragDismiss();
    Toast
    ============================================================ */
 let toastHandle = null;
+function dismissToast() {
+  clearTimeout(toastHandle);
+  $("#toast").classList.remove("show");
+}
 function toast(em, msg) {
+  if (!state.onboarded || document.body.classList.contains("sheet-open")) return;
   $("#toast .em").textContent = em;
   $("#toast-msg").textContent = msg;
   $("#toast").classList.add("show");
   clearTimeout(toastHandle);
-  toastHandle = setTimeout(() => $("#toast").classList.remove("show"), 3800);
+  toastHandle = setTimeout(() => $("#toast").classList.remove("show"), 2800);
 }
 
 /* ============================================================
